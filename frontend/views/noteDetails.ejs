<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= note.introTitle %></title>
     <!-- bootsrap -->
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
   <!-- css -->
    <link rel="stylesheet" href="/css/notes.css">
    <link rel="stylesheet" href="/css/styles.css">
    <!-- font -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

   
</head>
<body>
 
    <%- include('partials/navbar') %>


    <div class="container">
        <h1><%= noteInfo.title %></h1>
        <center><p><%= note.notesCode %></p></center>

        <div class="section">
            <h2> <%= note.introTitle %> </h2>

            <% note.units.forEach(unit => { %>
            <div class="content-box">
                <a href="<%= unit.pdfLink %>">
                    <p><%= unit.unitTitle %></p>
                </a>
               
                <div class="owner">Owner:  <%= unit.ownerName %></div>
            </div>
            <% }) %>

            <div class="subsection">
                <h3>Last 6 Year Question Paper and Quantum</h3>
              
                <!-- Display PYQ if available -->
                <% if (noteInfo.pyqLink && noteInfo.pyqTitle) { %>
                  <div class="content-box">
                    <a href="<%= noteInfo.pyqLink %>">
                      <p><%= noteInfo.pyqTitle %></p>
                    </a>
                  </div>
                <% } %>
              
                <!-- Display Quantum if available -->
                <% if (noteInfo.quantumLink && noteInfo.quantumTitle) { %>
                  <div class="content-box">
                    <a href="<%= noteInfo.quantumLink %>">
                      <p><%= noteInfo.quantumTitle %></p>
                    </a>
                  </div>
                <% } %>
              </div>
              



        </div>

      



       
    </div>

    <%- include('partials/footer') %>














</body>
</html>
